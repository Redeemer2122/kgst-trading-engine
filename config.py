"""
config.py — центральный файл настроек проекта KGST Monitor
Все конфигурационные константы собраны здесь для удобства редактирования.
"""

import os
from dotenv import load_dotenv

# Загружаем переменные из .env файла, если он существует
load_dotenv()

# ─── Telegram ──────────────────────────────────────────────────────────────────
TELEGRAM_TOKEN = os.environ.get("TELEGRAM_TOKEN")
TELEGRAM_CHAT_ID = os.environ.get("TELEGRAM_CHAT_ID")

# ─── Пороги и интервалы ────────────────────────────────────────────────────
# Порог профита для алерта (в %)
PROFIT_ALERT_THRESHOLD_PCT = 1.0

# Минимальный интервал между уведомлениями (сек), чтобы не спамить
ALERT_COOLDOWN = 10 * 60  # 10 минут

# ─── Курс USD / KGS ─────────────────────────────────────────────────────────
# Дефолтный курс KGS (fallback при первом запуске)
DEFAULT_KGS_RATE = 89.5

# URL для парсинга курса доллара
AKCHABAR_URL = "https://www.akchabar.kg/ru/exchange-rates/dollar/"

# ─── Интервалы обновления (в секундах) ──────────────────────────────────────
PRICE_UPDATE_INTERVAL = 30      # Цена KGST обновляется каждые 30 сек
RATE_UPDATE_INTERVAL = 5 * 60   # Курс USD обновляется каждые 5 мин

# ─── Комиссии сети за вывод (в USDT) ────────────────────────────────────────
FEE_TRC20 = 1.0    # TRC-20 (Tron)
FEE_BEP20 = 0.3    # BEP-20 (BSC)

# ─── Мульти-объёмный анализ ──────────────────────────────────────────────────
VOLUMES_KGS = [2000, 15000, 50000]

# Сумма инвестиции в сомах (основная сумма, для обратной совместимости)
INVESTMENT_KGS = 50_000

# ─── Путь к базе данных ──────────────────────────────────────────────────────
DB_PATH = "history.db"